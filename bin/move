#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

board=[]
9.times{board<<" "}

9.times{
display_board(board)
ind1=gets.strip
ind=input_to_index(ind1)
board=move(board,ind)
}
